import React from 'react';
declare type Props = {
    children: string;
    substrLength?: number;
};
export declare const Address: React.MemoExoticComponent<({ children: address, substrLength }: Props) => JSX.Element>;
export {};
